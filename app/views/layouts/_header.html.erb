<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">TTT</a>
    </div>
  

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
       <li role="presentation" <%= current_page?(root_path) ? "class=active" :""%>><%= link_to 'Home', root_path %></li>
        <li role="presentation" <%= current_page?(controller: 'static_pages', action: 'about' ) ? "class=active" :""%>><%= link_to 'About Us', static_pages_about_path %></li>
        <li role="presentation" <%= current_page?(controller: 'static_pages', action: 'contact') ? "class=active" :""%>><%= link_to 'Contact Us', static_pages_contact_path %></li>
        <li role="presentation" <%= current_page?(controller: 'users', action: 'index' ) ? "class=active" :""%>><%= link_to 'Users List', users_path %></li>
        <li role="presentation" <%= current_page?(controller: 'users', action: 'new' ) ? "class=active" :""%>><%= link_to 'Sign Up', new_user_path %></li>
        
        
        <li><%=
          if current_user_logged_in?
            the_link = link_to 'Sign Out', sign_out_path, method: :delete
          else
            the_link = link_to 'Sign In', sign_in_path
            
          end
         %></li>
        
      </ul>
    
     
     
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>